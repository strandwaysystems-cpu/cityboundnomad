node_modules/
dist/
.astro/
.DS_Store
*.log

# Never commit environment files — the Manus export this site was ported from
# shipped live database and API credentials in .project-config.json.
.env
.env.*
.project-config.json

# Raw photo exports fed to tools/import-photos.mjs. The originals stay in Apple
# Photos; only the converted, EXIF-stripped .webp files under
# public/images/places/ belong in the repo.
/photo-export/
/*-export/
